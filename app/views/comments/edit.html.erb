<h1>Edit Comments</h1>

<%= form_with model: @comment, url: article_comment_path, local: true do |form| %>

	<p>
		<%= form.label :content %><br>
		<%= form.text_field :content %>
	</p>

	<p>
		<%= form.submit "Edit" %>
	</p>

<% end %>

<%= link_to 'Back', article_comments_path(@article) %>