<h1>Welcome to WDI Instagram</h1>

 <%= link_to 'New Photo', new_article_path %>


 <table>
	 <tr>
		 <th>Author</th>
		 <th>Photo URL</th>
		 <th>Title</th>
	 </tr>

	 	 <% @articles.each do |post| %>
		 <tr>
			 <td><%= article.author %></td>
			 <td><%= article.photo_url %></td>
			 <td><%= article.title %></td>
			 <td><%= link_to 'Show', article_path(result) %></td>
			 <td><%= link_to 'Edit', edit_article_path(result) %></td>
			 <td><%= link_to 'Delete', article_path(results),
				 method: :delete,
				 data: { confirm: 'Are you sure?' } %></td>
		 </tr>
	 <% end %>
 </table>