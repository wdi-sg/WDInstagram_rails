<%= link_to 'InstaFake', controller: 'posts' %>

<h1># Hashtags #</h1>

<table>
	<tr>
		<th>Tag</th>
		<th></th>
	</tr>

	<% @tags.each do |tag| %>
		<tr>
			<td>#<%= tag.content %></td>
			<td><%= link_to 'Show posts', tag_path(tag) %></td>
		</tr>
	<% end %>
</table>


