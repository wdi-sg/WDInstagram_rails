<div>

	<%= image_tag ("#{@post.photo_url}") %>
	<p><%= @post.author %></p>
	<p><%= @post.date_taken %></p>
	<h3><%= @post.caption %></h3>

	<%= link_to 'Edit', edit_post_path(@post) %>
	<%= link_to 'Back', posts_path %>

</div>
