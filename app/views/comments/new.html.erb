<div class="container">
	<h1> Create form</h1>
	<%= form_with scope: :comment, url: entry_comments_path, local: true do |form| %>
	  <p>
	    <%= form.label :comment %><br/>
	    <%= form.text_area :commentContent %>
			<%= form.hidden_field :entry_id, value:@entry.id %><br/>
			<%= form.submit %>
	  </p>
	​

	<% end %>
</div>

