    <div>
        <strong>Author Name:</strong> <%= @post.author_name %>
    </div>
    <div>
        <img src=<%=@post.photo_url %> width="280" height="280" />
    </div>
    <div>
        <strong>Image URL:</strong> <%= @post.photo_url%>
    </div>
    <div>
        <strong>Caption:</strong> <%= @post.caption %>
    </div>
    <div>
        <%= link_to 'Edit', edit_post_path(@post) %>
        <%= link_to 'Destroy', post_path(@post),
            method: :delete,
            data: { confirm: 'Are you sure?' } %>
        <%= link_to 'Back', root_path %>
    </div>